GIT-->>>
git config --global user.name "Marcos Santos"
git config --global user.email marcos.santos@imed.edu.br
    


untracked-- 
a-- adicionado na árvore do git

git add --all
git add .



criar uma versão---- git commit -m "Versão inicial"= entregar uma versão até 
o ponto em que foi configurada

git log--histórico de alteração --- comitou(commit)

commit -m gravação de novas mudanças!!!!



voltar código 
git checkout e a chave 

retornar o ultimo código criado

git checkout master 

branch 
git remote add origin 


git push origin master = pega a branch master do pc e manda pra fora, para o
servidor remoto.
--master ou main--








GIT-->>>


